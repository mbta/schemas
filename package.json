{
  "scripts": {
    "format": "prettier --write .",
    "test:format": "prettier --check .",
    "test:compile": "ajv compile --spec=draft2020 -s \"schemas/*.json\"",
    "test:validate": "for dir in $(ls -d examples/* | xargs -n 1 basename); do ajv validate --spec=draft2020 -s schemas/$dir.json -d examples/$dir/\"*.json\"; done",
    "test": "npm run test:format && npm run test:compile && npm run test:validate"
  },
  "devDependencies": {
    "ajv-cli": "5.0.0",
    "prettier": "2.8.4"
  }
}
