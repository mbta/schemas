{
  "scripts": {
    "format": "prettier --write .",
    "test:format": "prettier --check .",
    "test": "npm run test:format"
  },
  "devDependencies": {
    "ajv-cli": "5.0.0",
    "prettier": "2.8.4"
  }
}
