---
sidebar_label: OCS
sidebar_position: 1
---

import DocCard from "@theme/DocCard";

# OCS Events

Events from OCS, published by [Trike](https://github.com/mbta/trike).

OCS provides Trike with raw messages, which Trike then turns into events, in two ways:

1. For all events, Trike will emit a `com.mbta.ocs.raw_message` message, which contains the original comma-separated raw OCS message. The details of this format are documented [here in the RTR repo](https://github.com/mbta/rtr/blob/master/docs/ocs_messages.md).

2. For some event types, Trike will also parse out relevant fields and emit additional messages in a friendlier JSON format.

## Events

### raw_message

Published for all messages received from OCS.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.raw_message",
    href: "/events/ocs/com.mbta.ocs.raw_message",
    docId: "events/ocs/com.mbta.ocs.raw_message",
  }}
/>

### TMOV messages

TMOV messages are emitted to indicate that a specific vehicle has physically moved (or has been "deleted", having exited the track systems). The available information differs between the heavy and light rail systems, and so we emit three different types of TMOV events, one for heavy rail movement, one for light rail movement, and one for train deletions.

#### tmov_heavy_rail

Published as trains move through the heavy rail system.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tmov_heavy_rail.v1",
    href: "/events/ocs/com.mbta.ocs.tmov_heavy_rail.v1",
    docId: "events/ocs/com.mbta.ocs.tmov_heavy_rail.v1",
  }}
/>

#### tmov_light_rail

Published as trains move through the light rail system.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tmov_light_rail.v1",
    href: "/events/ocs/com.mbta.ocs.tmov_light_rail.v1",
    docId: "events/ocs/com.mbta.ocs.tmov_light_rail.v1",
  }}
/>

#### tmov_deleted

Published as trains are deleted from the OCS (ie, exit the heavy or light rail systems.)

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tmov_deleted.v1",
    href: "/events/ocs/com.mbta.ocs.tmov_deleted.v1",
    docId: "events/ocs/com.mbta.ocs.tmov_deleted.v1",
  }}
/>

### TSCH messages

TSCH messages are emitted to convey information about scheduled trips.

#### tsch_rld

Published when OCS reloads the schedule, ie dumps existing schedule information and starts fresh.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_rld.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_rld.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_rld.v1",
  }}
/>

#### tsch_new

Published when a new trip is created. This may be a scheduled trip loaded from Hastus, or added by a dispatcher, or auto-generated by the system.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_new.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_new.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_new.v1",
  }}
/>

#### tsch_del

Published when a trip is deleted, or when a previously deleted trip is "undeleted".

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_del.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_del.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_del.v1",
  }}
/>

#### tsch_lnk

Published to update the previous or next trip associated with a given trip, ie "linking" multiple trips together.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_lnk.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_lnk.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_lnk.v1",
  }}
/>

#### tsch_off

Published when a scheduled departure time is adjusted, by adding a positive or negative offset of minutes.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_off.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_off.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_off.v1",
  }}
/>

#### tsch_con

Published when there is an update to a train's consist, ie the set of numbered cars that comprise said train.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_con.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_con.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_con.v1",
  }}
/>

#### tsch_dst

Published when there is a change to a train's destination, route, or scheduled arrival time.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_dst.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_dst.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_dst.v1",
  }}
/>

#### tsch_asn

Published when a train is assigned to a trip.

<DocCard
  item={{
    type: "link",
    label: "com.mbta.ocs.tsch_asn.v1",
    href: "/events/ocs/com.mbta.ocs.tsch_asn.v1",
    docId: "events/ocs/com.mbta.ocs.tsch_asn.v1",
  }}
/>

## Shared Types

Types used by multiple of the events are documented here. Types used by only one of the events are documented on each event page.

### RFC3339 Timestamp

All timestamps MUST have a timezone offset, as required by RFC3339. The timestamp SHOULD be in UTC with an offset of "Z".

> _Example: `"2025-01-01T17:00:00Z"` for noon eastern time._

## Details

Events are in the [CloudEvents](https://github.com/cloudevents/spec/blob/v1.0.2/cloudevents/spec.md) format, and published via AWS Kinesis. See [RFC 19 Event Driven Architecture](https://github.com/mbta/technology-docs/blob/main/rfcs/accepted/0019-event-driven-architecture.md) for more about TID's approach to publishing events.

### Kinesis

- TODO: Verify plan for Kinsesis stream and Trike environments

Events will be written as records to a Kinesis stream. Each Trike environment (`dev`, `dev-green`, and `prod`) will have a separate stream, named `tid-trike-<environment>`.

Multiple events can be included in a single Kinesis record, by wrapping them in a JSON array. This is an optimization for improving write speeds: further steps in the Kinesis pipeline may break up or rearrange these arrays.

Kinesis only adds a limited (&lt;1s) latency between when the event is generated and when it is available to consumers.

### Short-term storage

- TODO: Figure out if OCS events are relevant 24 hours in the future.
- TOOD: Confirm rules/caveats here make sense and provide examples.

Events are only maintained in Kinesis for 24 hours. Some events (EXAMPLE?) can affect trips more than 24 hours in the future. Clients SHOULD maintain records internally of these future changes. Clients MUST NOT expect that reading the Kinesis stream from the trim horizon will return all events affecting the current service day. Clients MUST tolerate receiving updates for events that reference unseen past events (EXAMPLE?).

### Long-term storage

- TODO: Do we plan to add additional long-term storage beyond OCS Saver?
- TODO: Confirm there is no PII concerns for these events.

OCS raw events are currently stored by OCS Saver.

### Compatibility

These event schemas may be updated, according to [RFC 19 Event Driven Architecture #Compatibility](https://github.com/mbta/technology-docs/blob/main/rfcs/accepted/0019-event-driven-architecture.md#compatibility).

A change is backwards-compatible if all old events are valid with the new schema, all new events are valid with the old schema, and their semantics have not changed. In that case, Trike MAY publish events with the new format without warning, and the event version number will not be bumped.

In the case of a backwards-incompatible change, a new event `type` MUST be created (probably by bumping the `v1` version number). Trike MUST continue to generate old events alongside the new events until no more consumers depend on the old version.

In either case, this documentation MUST be updated.

Examples of backwards-compatible changes:

- Adding a new event `type` (which is not replacing an existing event `type`).

#### Consumer Responsibilities

- TODO: Verify that existing consumers follow the rules here.

- Consumers MUST ignore event `type`s that they do not understand.
- Consumers MUST ignore fields in events (and in nested objects in events) which they do not understand, trusting that any additional fields are not required to process the event.
- Consumers SHOULD ignore earlier versions of the same event if they can parse a later version (i.e. ignoring `v1` events if they can parse `v2`).

### Idempotence

- TODO: Maybe add notes about the sequence `counter` that exists within OCS events?

All Glides events are idempotent (and to remove that promise would be a non backwards compatible change).

If an event is submitted to the stream twice, due to a network error, then the events will have the same event id, and the same data. Clients MAY ignore duplicate events.
